<nav class="bg-white shadow-md">
    <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">

        <a href="/" class="text-2xl font-bold text-[#FF5A5F]">
            Airbnb
        </a>

        <div class="flex gap-6">
            <a href="/homes"
            class="<%= currentPath === '/homes' ? 'text-[#FF5A5F] font-bold' : 'text-gray-700' %>">
                Home
            </a>

             <a href="/bookings"
            class="<%= currentPath === '/bookings' ? 'text-[#FF5A5F] font-bold' : 'text-gray-700' %>">
                Bookings
            </a>

            <a href="/favourites"
            class="<%= currentPath === '/favourites' ? 'text-[#FF5A5F] font-bold' : 'text-gray-700' %>">
                Favourites
            </a>

            <a href="/host/add-home"
            class="<%= currentPath === '/host/add-home' ? 'text-[#FF5A5F] font-bold' : 'text-gray-700' %>">
                Add Home
            </a>

            <a href="/host/homes"
            class="<%= currentPath === '/host/homes' ? 'text-[#FF5A5F] font-bold' : 'text-gray-700' %>">
                Host Homes
            </a>
        </div>

    </div>
</nav>